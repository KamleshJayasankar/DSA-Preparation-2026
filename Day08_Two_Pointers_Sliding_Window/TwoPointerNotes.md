# Two Pointer Notes

Two pointers maintain two positions in the input.

For a sorted array and a target pair sum:
- If the sum is too small, move the left pointer right.
- If the sum is too large, move the right pointer left.
- If the sum matches, the pair is found.
