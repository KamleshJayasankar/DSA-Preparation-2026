# Day 11 - Complexity

- Frequency counting with HashMap: O(n) average time, O(k) space for k distinct values.
- Duplicate detection with HashSet: O(n) average time, O(n) space in the worst case.
- Two Sum with HashMap: O(n) average time, O(n) space.
