# Day 13 - Complexity

Binary tree traversal:
- Time: O(n), because every node may be visited once.
- Recursive space: O(h), where h is tree height.

BST search/insertion:
- Average time: O(log n) when reasonably balanced.
- Worst-case time: O(n) when the tree becomes skewed.
