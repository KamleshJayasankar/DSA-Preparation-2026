# Day 06 - Complexity Notes

Basic palindrome two-pointer check: O(n) time and O(1) extra space.

The simple pattern-matching implementation may compare characters at multiple starting positions, giving O(n*m) worst-case time for text length n and pattern length m.
