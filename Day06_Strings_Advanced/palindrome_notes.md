# Palindrome Notes

A palindrome reads the same from both directions.

A two-pointer check starts at the first and last characters and moves inward. If a mirrored pair differs, the string is not a palindrome.
