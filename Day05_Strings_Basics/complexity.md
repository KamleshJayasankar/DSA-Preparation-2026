# Day 05 - Complexity Notes

For a basic String operation, the actual cost depends on the operation and the size of the string.

When analyzing a string problem, consider how many characters are inspected and whether a new String is produced.
