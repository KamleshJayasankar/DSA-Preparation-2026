# Day 04 - Complexity

- Recursive factorial: O(n) time, O(n) call-stack space.
- Recursive maximum over n elements: O(n) time, O(n) call-stack space.
- Subset generation: up to 2^n subsets and O(n) recursion depth; output size also contributes to total running time.
